interface Props {
  apiKey: string;
   issuerId: string; keyId: string;
    setApiKey: (newApiKey: string) => void; setIssuerId: (newIssuerId: string) => void,
     setKeyId: (newKeyId: string) => void;
}

export default Props;
